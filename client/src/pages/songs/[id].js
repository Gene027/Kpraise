import React from 'react'
import { useDispatch, useSelector } from 'react-redux';
import { Error, Loader, Card } from '../../components';
import { useGetSongsByGenreQuery } from '../../redux/services/api';

const SongDetails = () => {
  return (
    <div>Song Detail</div>
  )
}

export default SongDetails